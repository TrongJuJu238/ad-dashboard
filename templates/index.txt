<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>AD Dashboard - AI Planner</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=JetBrains+Mono&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/animejs/3.2.1/anime.min.js"></script>

</head>
<body>
    <div class="layout">
        <aside class="sidebar">
            <div class="logo">
                <i class="fas fa-shield-alt" style="color: #60a5fa; font-size: 24px;"></i>
                <span>AD Management</span>
            </div>
            <nav class="menu">
                <a href="/" class="menu-item {% if tab == 'user' %}active{% endif %}">
                    <i class="fas fa-user-shield"></i>
                    <span>Users</span>
                </a>
                <a href="/computer" class="menu-item {% if tab == 'computer' %}active{% endif %}">
                    <i class="fas fa-desktop"></i>
                    <span>Computers</span>
                </a>
                <a href="/task" class="menu-item {% if tab == 'task' %}active{% endif %}">
                    <i class="fas fa-list-check"></i>
                    <span>Tasks</span>
                </a>
                <a href="/calendar" class="menu-item {% if tab == 'calendar' %}active{% endif %}">
                    <i class="fas fa-calendar-alt"></i>
                    <span>Calendar</span>
                </a>
                <div class="menu-item">
                    <i class="fas fa-history"></i>
                    <span>Audit Logs</span>
                </div>
                <div class="menu-item">
                    <i class="fas fa-cog"></i>
                    <span>Settings</span>
                </div>
            </nav>
        </aside>

        <main class="main">
            <header class="top-nav">
                <form method="POST" class="search-tasks">
                    <i class="fas fa-search" style="color: var(--text-muted);"></i>
                    <input type="text" name="keyword" placeholder="Search AD..." value="{{ keyword or '' }}" autocomplete="off">
                    <button type="submit" style="display:none"></button>
                </form>
            </header>

            <div class="dashboard-header">
                <h1>{{ "User Management" if tab == 'user' else ("Computer Management" if tab == 'computer' else ("Task Management" if tab == 'task' else "Calendar View")) }}</h1>
                <p>Manage your {{ 'Active Directory ' if tab != 'task' and tab != 'calendar' else '' }}{{ tab }}s efficiently.</p>
            </div>

            <section class="stats-grid animate-stats">
                {% if tab == 'task' %}
                <div class="stat-card active-tasks">
                    <div class="stat-info">
                        <h4>Active Tasks</h4>
                        <p>{{ stats.active }}</p>
                    </div>
                    <i class="fas fa-tasks" style="color: var(--primary);"></i>
                </div>
                <div class="stat-card completed">
                    <div class="stat-info">
                        <h4>Completed</h4>
                        <p>{{ stats.completed }}</p>
                    </div>
                    <i class="fas fa-check-circle" style="color: var(--accent-green);"></i>
                </div>
                <div class="stat-card in-progress">
                    <div class="stat-info">
                        <h4>In Progress</h4>
                        <p>{{ stats.in_progress }}</p>
                    </div>
                    <i class="fas fa-clock" style="color: var(--accent-orange);"></i>
                </div>
                <div class="stat-card todo">
                    <div class="stat-info">
                        <h4>To Do</h4>
                        <p>{{ stats.todo }}</p>
                    </div>
                    <i class="fas fa-exclamation-circle" style="color: var(--accent-red);"></i>
                </div>
                {% else %}
                <div class="stat-card active-tasks">
                    <div class="stat-info">
                        <h4>Total {{ tab|capitalize }}s</h4>
                        <p>{{ users|length if tab == 'user' else computers|length }}</p>
                    </div>
                    <i class="fas fa-database" style="color: var(--primary);"></i>
                </div>
                {% if tab == 'user' %}
                <div class="stat-card todo">
                    <div class="stat-info">
                        <h4>Locked Users</h4>
                        <p>{{ users|selectattr("LockedOut")|list|length }}</p>
                    </div>
                    <i class="fas fa-user-lock" style="color: var(--accent-red);"></i>
                </div>
                {% endif %}
                <div class="stat-card in-progress">
                    <div class="stat-info">
                        <h4>Search Result</h4>
                        <p>{{ keyword or 'All' }}</p>
                    </div>
                    <i class="fas fa-search" style="color: var(--accent-orange);"></i>
                </div>
                {% endif %}
            </section>

            <div class="content-grid">
                <section class="panel animate-panel">
                    <div class="panel-header">
                        <h2>{{ tab|capitalize }} List</h2>

                        {% if tab == "computer" %}
                        <div class="sort-container">
                            <select id="sortField" class="sort-select">
                                <option value="Name">Name</option>
                                <option value="Department">Department</option>
                                <option value="IP">IP Status</option>
                                <!--                                <option value="Status">Status</option>
            -->
                            </select>

                            <button class="sort-direction" onclick="toggleSortDirection()" id="sortDirection">
                                ?
                            </button>

                            <button class="btn-new-task" onclick="applyComputerSort()">
                                Sort
                            </button>
                        </div>
                        {% endif %}
                        {% if tab == 'task' %}
                        <button class="btn-new-task" onclick="document.getElementById('taskModal').style.display='flex'">+ New Task</button>
                        {% endif %}
                    </div>

                    {% if tab == "user" %}
                    <table class="task-table">
                        <thead>
                            <tr class="animate-row" >
                                <th>Name / ID</th>
                                <th>Description</th>
                                <th>Status</th>
                                <th>OU / Email</th>
                                <th>Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for user in users %}
                            <tr class="animate-row">
                                <td>
                                    <div style="font-weight:600">{{ user.DisplayName }}</div>
                                    <div style="font-size:12px; color:var(--text-muted)">#{{ user.employeeID }}</div>
                                </td>
                                <td>{{ user.Description }}</td>
                                <td>
                                    {% if user.LockedOut %}
                                    <span class="badge high">Locked</span>
                                    {% else %}
                                    <span class="badge low">Active</span>
                                    {% endif %}
                                </td>
                                <td>
                                    <div style="font-size:13px">{{ user.DepartmentOU }}</div>
                                    <div style="font-size:11px; color:var(--text-muted)">{{ user.Mail or 'No Mail' }}</div>
                                </td>
                                <td>
                                    {% if user.LockedOut %}
                                    <button class="btn-new-task" style="padding: 4px 8px; font-size: 12px;"
                                            onclick="if(confirm('Unlock {{ user.DisplayName }}?')) unlockUser('{{ user.SamAccountName }}')">
                                        Unlock
                                    </button>
                                    {% else %}
                                    <i class="fas fa-check" style="color: var(--accent-green)"></i>
                                    {% endif %}
                                </td>
                            </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                    {% elif tab == "task" %}
                    <table class="task-table">
                        <thead>
                            <tr class="animate-row">
                                <th>Task</th>
                                <th>Priority</th>
                                <th>Due Date</th>
                                <th>Status</th>
                                <th>Action</th>
                            </trclass="animate-row">
                        </thead>
                        <tbody>
                            {% for task in tasks %}
                            <tr class="animate-row">
                                <td style="font-weight:600">{{ task.task }}</td>
                                <td>
                                    <span class="badge {{ task.priority.lower() }}">{{ task.priority }}</span>
                                </td>
                                <td style="color: var(--text-muted)">{{ task.due_date }}</td>
                                <td>
                                    <span class="badge status" style="background: rgba(14, 165, 233, 0.1); color: var(--primary); border: 1px solid rgba(14, 165, 233, 0.2)">
                                        {{ task.status }}
                                    </span>
                                </td>
                                <td>
                                    <i class="fas fa-ellipsis-v" style="color: var(--text-muted); cursor: pointer"></i>
                                </td>
                            </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                    {% else %}
                    <table class="task-table">
                        <thead>
                            <tr class="animate-row">
                                <th>Computer Name</th>
                                <th>IP Address</th>
                                <th>Department / OU</th>
                                <th>OU</th>
                                <th>Description</th>
                                <th>Remote</th>
                            </trclass="animate-row">
                        </thead>
                        <tbody>
                            {% for pc in computers %}
                            <tr class="animate-row">
                                <td style="font-weight:600">{{ pc.Name }}</td>
                                <td class="ip" style="color: var(--accent-orange)">{{ pc.IP }}</td>
                                <td>
                                    <span class="badge status">{{ pc.Department }}</span>

                                </td>
                                <td>
                                    <div style="font-size:11px;">{{ pc.OU }}</div>
                                </td>
                                <td>{{ pc.Description }}</td>
                                <td>
                                    <button class="btn-new-task" style="font-size: 12px;"
                                            onclick="fetch('/remote/{{ pc.Name }}')">
                                        <i class="fas fa-desktop"></i>
                                    </button>
                                </td>
                            </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                    {% endif %}
                </section>

                {% if tab == 'task' %}
                <div class="calendar-widget animate-calendar">
                    <div class="calendar-widget-header">
                        <h3>Calendar</h3>
                        <div class="calendar-widget-nav">

                            <a href="{{ url_for('task_bp.task_page', month=prev_month, year=prev_year) }}">
                                <i class="fas fa-chevron-left"></i>
                            </a>

                            <span>{{ current_month }}</span>

                            <a href="{{ url_for('task_bp.task_page', month=next_month, year=next_year) }}">
                                <i class="fas fa-chevron-right"></i>
                            </a>

                        </div>
                    </div>
                   

                    <div class="calendar-widget-grid">

                        <div class="calendar-widget-day-label">Sun</div>
                        <div class="calendar-widget-day-label">Mon</div>
                        <div class="calendar-widget-day-label">Tue</div>
                        <div class="calendar-widget-day-label">Wed</div>
                        <div class="calendar-widget-day-label">Thu</div>
                        <div class="calendar-widget-day-label">Fri</div>
                        <div class="calendar-widget-day-label">Sat</div>

                        {# chèn ô tr?ng ??u tháng #}
                        {% for _ in range(first_weekday) %}
                        <div></div>
                        {% endfor %}

                        {# render ngày #}
                        {% for day in range(1, days_in_month + 1) %}
                        <div class="calendar-widget-day {% if day == today %}today{% endif %}">
                            {{ day }}
                        </div>
                        {% endfor %}

                    </div>
                </div>
                {% else %}
                <aside class="panel calendar-panel animate-system-panel">
                    <div class="calendar-header">
                        <i class="fas fa-calendar-day" style="color: var(--primary)"></i>
                        <span style="font-weight: 600;">System Overview</span>
                    </div>
                    <div style="margin-top: 20px;">
                        <div style="background: rgba(255,255,255,0.03); padding: 15px; border-radius: 12px; margin-bottom: 10px;">
                            <div style="font-size: 12px; color: var(--text-muted)">Domain</div>
                            <div style="font-weight: 600">htpvtg.local</div>
                        </div>
                        <div style="background: rgba(255,255,255,0.03); padding: 15px; border-radius: 12px;">
                            <div style="font-size: 12px; color: var(--text-muted)">Status</div>
                            <div style="font-weight: 600; color: var(--accent-green)">Connected</div>
                        </div>
                    </div>
                </aside>
                {% endif %}
            </div>
        </main>
    </div>


    <script src="{{ url_for('static', filename='app.js') }}"></script>
</body>
</html>

